# WeChatOptimized 稳定性优化计划

> **核心原则**：回归原项目实现，减少过度封装，确保可行性和稳定性。

---

## 一、当前问题诊断

### 1.1 连接问题（已修复）

| 问题 | 原因 | 修复状态 |
|------|------|----------|
| AstrBot WebSocket 连不上 | 缺少 `X-Self-ID` 等 Headers | ✅ 已修复 |
| WeFlow 检查返回 400 | 调用了需要 `talker` 参数的 API | ✅ 已修复 |
| 启动时配置未生效 | 用 `get_config()` 而非 `load_config()` | ✅ 已修复 |

### 1.2 架构问题（待优化）

| 问题 | 当前实现 | 原项目实现 | 风险 |
|------|----------|------------|------|
| 过度封装 | 20个文件，层层嵌套 | 扁平结构，6-8个文件 | 调试困难、出错概率高 |
| 发送器复杂化 | 优先级队列 + 快速通道 + 人类模拟 | 简单队列 + 锁保护 | 逻辑复杂，边界情况多 |
| WebSocket 库混用 | `websocket-client` + `websockets` | 纯 `websockets` | 行为不一致 |
| 状态管理分散 | `AppState` 单例 + 模块变量 | 全局模块变量 | 状态不同步 |

### 1.3 遗漏功能

- [ ] 暂停/恢复功能（原项目有，当前缺失）
- [ ] 日志文件输出（只有控制台）
- [ ] 图片发送的坐标后备模式细节

---

## 二、优化方案

### 2.1 文件结构重组

**目标**：回归原项目的扁平结构，减少文件数量

```
WeChatOptimized/
├── main.py                 # 入口 + 生命周期管理（参考 Akasha main.py）
├── config.py               # 配置加载（参考 Akasha config.py）
├── state.py                # 全局状态（参考 Akasha state.py）
├── bridge_core.py          # WeFlow 桥接核心（参考 Akasha bridge_core.py）
├── ob_client.py            # OneBot WebSocket 客户端（参考 Akasha ob_client.py）
├── ob_protocol.py          # OneBot 协议处理（参考 Akasha ob_protocol.py）
├── sender.py               # 发送器（合并 uia_sender + senders，参考 Akasha）
├── web_panel.py            # Web 控制面板（参考 Akasha web_panel.py）
├── mouse_wanderer.py       # 鼠标漫游（来自 WeeMessenger）
├── config.yaml             # 配置文件
├── requirements.txt        # 依赖
└── 启动.bat                # 启动脚本
```

**删除的文件**：
- `src/` 目录结构（过度封装）
- `src/anti_detect/` 子目录
- `src/bridge/` 子目录
- `src/network/` 子目录
- `src/web/` 子目录
- `src/ai/image_caption.py`（暂不需要）
- `src/voice_sender.py`（暂不需要）

### 2.2 核心模块重写计划

#### 2.2.1 `state.py` — 参考 Akasha 原项目

```python
# 关键改动：
# 1. 使用模块级变量而非类单例
# 2. _wxid_to_int 使用 hash() 而非 md5

import threading
from typing import Optional

# 状态控制
running = False
paused = threading.Event()
paused.clear()
run_lock = threading.Lock()
bridge_thread = None

# OneBot WebSocket
_ob_ws = None
_ob_ws_loop = None
_ob_ws_ready = threading.Event()
_self_id_int = 0

def _wxid_to_int(wxid: str) -> int:
    """参考 Akasha: 使用 hash 而非 md5"""
    return abs(hash(wxid)) % (2**31)

# 桥接实例
bridge_instance = None
bridge_lock = threading.Lock()
sender_instance = None
_ob_id_to_contact: dict[int, str] = {}
ob_client_started = False
group_reply_mode = "mention"
```

#### 2.2.2 `ob_client.py` — 参考 Akasha 原项目

```python
# 关键改动：
# 1. 使用 websockets 库的 async with 上下文管理器
# 2. 添加 X-Self-ID 等 Headers
# 3. 添加心跳保活
# 4. 使用 asyncio.create_task 异步处理 API

async def _ob_client_main():
    """参考 Akasha ob_client.py 的 _ob_client_main"""
    while state.running:
        try:
            async with websockets.connect(
                config.ASTRBOT_OB_URL,
                additional_headers={
                    "X-Self-ID": str(state._self_id_int),
                    "X-Client-Role": "Universal",
                    "User-Agent": "OneBot/11",
                }
            ) as ws:
                state._ob_ws = ws
                state._ob_ws_ready.set()
                # 心跳 + 消息处理...
```

#### 2.2.3 `bridge_core.py` — 参考 Akasha 原项目

```python
# 关键改动：
# 1. WeFlowBridge 类封装所有桥接逻辑
# 2. 消息缓冲在类内部管理
# 3. SSE 连接检查使用正确的 API

class WeFlowBridge:
    def __init__(self, sender):
        self.sender = sender
        self.processed_ids = set()
        self.start_timestamp = int(time.time())
        self.pending_buffers = {}
        # ...

    def listen_sse(self):
        """直接参考 Akasha bridge_core.py 的 listen_sse 方法"""
        # 使用 SSE 端点而非 REST API 做连接检查
        sse_url = f"{config.WE_FLOW_BASE_URL}/api/v1/push/messages?access_token={config.ACCESS_TOKEN}"
        # ...
```

#### 2.2.4 `sender.py` — 合并 Akasha + WeeMessenger

```python
# 关键改动：
# 1. 简化为同步调用 + 锁保护（参考 Akasha uia_sender.py）
# 2. 移除优先级队列（过度设计）
# 3. 保留鼠标漫游（来自 WeeMessenger mouse_wanderer.py）
# 4. 保留 ValuePattern 优化

class UiaSender:
    def __init__(self, search_enabled=True):
        self._lock = threading.Lock()
        self._window = None
        self._input_control = None
        # 鼠标漫游（可选）
        self.wanderer = MouseWanderer()
        # ...

    def send_text(self, contact: str, text: str) -> bool:
        """参考 Akasha uia_sender.py 的 send_text 方法"""
        with self._lock:
            # 激活窗口 → 切换联系人 → 定位输入框 → 发送
            # ...
```

#### 2.2.5 `main.py` — 参考 Akasha 原项目

```python
# 关键改动：
# 1. 启动时立即初始化状态
# 2. _start_bridge / _stop_bridge 参考 Akasha
# 3. _bridge_loop 包含重连逻辑

def _start_bridge():
    """参考 Akasha main.py 的 _start_bridge"""
    with state.run_lock:
        if state.running:
            return
        state.running = True
    state.paused.clear()
    state.sender_instance = create_sender()
    # 启动 OB 客户端线程
    # 启动桥接循环线程
    # ...

def _stop_bridge():
    """参考 Akasha main.py 的 _stop_bridge"""
    # 关闭 SSE
    # 关闭 WebSocket
    # 重置状态
    # ...
```

### 2.3 配置系统简化

**当前问题**：YAML 嵌套结构 + 深度合并逻辑复杂

**优化方案**：使用 JSON 扁平结构（参考 Akasha config.json）

```json
{
    "weflow_base_url": "http://127.0.0.1:5031",
    "access_token": "xxx",
    "astrbot_ob_url": "ws://127.0.0.1:11229/ws",
    "bot_nicknames": ["机器人"],
    "bot_wxid": "wxid_xxx",
    "send_method": "uia",
    "buffer_seconds": 5,
    "group_reply_mode": "mention",
    "web_port": 8766
}
```

### 2.4 Web 面板简化

**当前问题**：侧边栏 + 多页面 + 统计页面（过度设计）

**优化方案**：单页面控制面板（参考 Akasha web_panel.py）

- 状态卡片
- 启停/暂停按钮
- 日志显示
- 配置编辑（折叠面板）

---

## 三、实施步骤

### Phase 1：核心修复（已完成）

- [x] 修复 WebSocket Headers
- [x] 修复 WeFlow 连接检查
- [x] 修复配置热加载

### Phase 2：架构重组（建议执行）

| 步骤 | 任务 | 参考文件 | 预计时间 |
|------|------|----------|----------|
| 1 | 创建扁平目录结构 | - | 5 min |
| 2 | 重写 `state.py` | Akasha state.py | 10 min |
| 3 | 重写 `config.py` | Akasha config.py | 10 min |
| 4 | 重写 `ob_client.py` | Akasha ob_client.py | 20 min |
| 5 | 重写 `bridge_core.py` | Akasha bridge_core.py | 30 min |
| 6 | 重写 `sender.py` | Akasha uia_sender.py + WeeMessenger mouse_wanderer.py | 30 min |
| 7 | 重写 `main.py` | Akasha main.py | 20 min |
| 8 | 重写 `web_panel.py` | Akasha web_panel.py | 20 min |
| 9 | 更新 `config.yaml` → `config.json` | Akasha config.json | 5 min |
| 10 | 测试验证 | - | 30 min |

**总计**：约 3 小时

### Phase 3：功能增强（可选）

- [ ] 添加暂停/恢复功能
- [ ] 添加日志文件输出
- [ ] 添加图片发送的完整测试
- [ ] 添加语音发送功能（来自 WeeMessenger）

---

## 四、关键代码参考

### 4.1 WebSocket 连接（Akasha ob_client.py 第 32-45 行）

```python
async with websockets.connect(
    config.ASTRBOT_OB_URL,
    additional_headers={
        "X-Self-ID": str(state._self_id_int),
        "X-Client-Role": "Universal",
        "User-Agent": "OneBot/11",
    }
) as ws:
    state._ob_ws = ws
    state._ob_ws_ready.set()
```

### 4.2 发送器锁保护（Akasha uia_sender.py 第 207-210 行）

```python
def send_text(self, contact: str, text: str) -> bool:
    with self._lock:
        if not self._ready:
            return False
        # ... 发送逻辑
```

### 4.3 SSE 连接（Akasha bridge_core.py 第 163-175 行）

```python
def listen_sse(self):
    sse_url = f"{config.WE_FLOW_BASE_URL}/api/v1/push/messages?access_token={config.ACCESS_TOKEN}"
    headers = {"Accept": "text/event-stream", "Cache-Control": "no-cache"}
    self._sse_session = requests.get(sse_url, headers=headers, stream=True, timeout=None)
    if self._sse_session.status_code != 200:
        log.error(f"连接失败: HTTP {self._sse_session.status_code}")
        return
```

### 4.4 鼠标漫游（WeeMessenger mouse_wanderer.py 第 24-45 行）

```python
@staticmethod
def _human_mouse_move(start_x, start_y, end_x, end_y):
    steps = random.randint(3, 12)
    # 随机权重分配
    weights = [random.random() for _ in range(steps)]
    # ... 贝塞尔曲线模拟
```

---

## 五、风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 重写引入新 Bug | 中 | 高 | 逐步测试，每个模块独立验证 |
| 原项目代码有隐藏问题 | 低 | 中 | 参考而非复制，理解逻辑后实现 |
| 配置格式变更导致兼容问题 | 低 | 低 | 提供配置迁移脚本 |
| 微信版本更新导致 UIA 失效 | 低 | 高 | 保留坐标后备方案 |

---

## 六、验收标准

1. **连接稳定性**
   - [ ] WeFlow SSE 连接成功并保持
   - [ ] AstrBot WebSocket 连接成功并保持
   - [ ] 断线自动重连

2. **消息收发**
   - [ ] 接收微信消息 → 推送到 AstrBot
   - [ ] AstrBot 回复 → 发送到微信
   - [ ] 群聊 @ 检测正常
   - [ ] 消息缓冲合并正常

3. **防检测**
   - [ ] 鼠标漫游正常工作
   - [ ] 发送时暂停漫游
   - [ ] 随机延迟正常

4. **Web 面板**
   - [ ] 状态显示正确
   - [ ] 启停按钮正常
   - [ ] 配置保存生效

---

## 七、结论

当前项目的核心问题是**过度封装**和**偏离原项目实现**。

建议采用 **Phase 2 架构重组**方案：
1. 回归扁平结构
2. 直接参考原项目代码实现
3. 只保留经过验证的功能

这样可以：
- 减少代码量 50%+
- 降低调试难度
- 提高稳定性
- 便于后续维护
